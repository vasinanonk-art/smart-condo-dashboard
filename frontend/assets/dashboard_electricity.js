(() => {
  'use strict';
  if (window.__dashboardElectricityInstalled) return;
  window.__dashboardElectricityInstalled = true;

  const electricityState = {payload:null,samples:[],lastSampleKey:null};
  const safe=value=>window.safeText?window.safeText(value):String(value??'');
  const valueOrNA=value=>value===null||value===undefined||value===''?'Not available':value;
  const SOURCE_LABELS={tuya_local:'Tuya Local',home_assistant:'Home Assistant',mqtt:'MQTT',unknown:'Unknown'};
  const DIAGNOSTIC_KEYS=['mapping_verified','stale','last_success','last_attempt_ts','last_error','consecutive_failures','configured_ip','runtime_ip','auto_discovery','last_scan_ts','last_scan_result','scan_count','poller_started','poller_alive'];

  function installUi(){document.querySelectorAll('.nav,.mobile-nav').forEach(host=>{if(host.querySelector('[data-nav="electricity"]'))return;const button=document.createElement('button');button.dataset.nav='electricity';button.dataset.short='EL';button.textContent='Electricity';const topology=host.querySelector('[data-nav="topology"]');topology?host.insertBefore(button,topology):host.appendChild(button);});if(document.querySelector('[data-page="electricity"]'))return;const section=document.createElement('section');section.className='page';section.dataset.page='electricity';section.innerHTML='<div id="electricityPage" class="electricity-page"><div class="card"><div class="empty">Electricity data is loading.</div></div></div>';document.querySelector('.main')?.appendChild(section);}
  function toEpoch(value){if(value===null||value===undefined||value===''||value===0||value==='0')return null;const numeric=Number(value);if(Number.isFinite(numeric))return numeric>1e12?Math.floor(numeric/1000):Math.floor(numeric);const parsed=Date.parse(value);return Number.isFinite(parsed)?Math.floor(parsed/1000):null;}
  function localTime(value){const ts=toEpoch(value);if(!ts)return'Not available';return new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Bangkok',year:'numeric',month:'short',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date(ts*1000));}
  function sourceLabel(value){const key=String(value||'unknown').toLowerCase();return SOURCE_LABELS[key]||key.split('_').map(part=>part.charAt(0).toUpperCase()+part.slice(1)).join(' ');}
  function healthLabel(payload){const health=String(payload?.health||'unknown').toLowerCase();const partial=health==='warning'&&['voltage','current','power','total_energy'].some(key=>payload?.[key]!==null&&payload?.[key]!==undefined);if(partial)return'Partial Data';return{healthy:'Healthy',warning:'Warning',offline:'Offline',unknown:'Unknown'}[health]||'Unknown';}
  function addSample(payload){const d=payload?.diagnostics||{},ts=toEpoch(payload?.last_update||d.last_success||d.last_attempt_ts)||Math.floor(Date.now()/1000),key=`${ts}:${payload?.power??''}:${payload?.voltage??''}:${payload?.current??''}`;if(key===electricityState.lastSampleKey)return;electricityState.lastSampleKey=key;electricityState.samples.push({ts,voltage:payload?.voltage,current:payload?.current,power:payload?.power});electricityState.samples=electricityState.samples.slice(-20);}
  async function loadElectricity(){try{const payload=await window.get('/api/electricity/status');electricityState.payload=payload;addSample(payload);}catch(error){console.error('Electricity refresh failed',{name:error?.name||'Error',message:error?.message||'Unknown error'});}}
  function metricCard(label,value,unit='',secondary=false){const display=valueOrNA(value),hasUnit=display!=='Not available'&&unit;return`<div class="electricity-metric${secondary?' secondary':''}"><span>${safe(label)}</span><strong>${safe(display)}${hasUnit?`<small>${safe(unit)}</small>`:''}</strong></div>`;}
  function badge(label,value,cls=''){return`<span class="electricity-badge ${cls}">${safe(label)} · ${safe(value)}</span>`;}
  function niceScale(value){const samples=electricityState.samples.map(item=>Number(item.power)).filter(Number.isFinite),observed=Math.max(Number(value)||0,...samples,1),padded=observed*1.25,magnitude=10**Math.floor(Math.log10(padded)),normalized=padded/magnitude,step=normalized<=1?1:normalized<=2?2:normalized<=5?5:10;return step*magnitude;}
  function loadLabel(power,max){if(!Number.isFinite(Number(power)))return'Load unavailable';const ratio=max>0?Number(power)/max:0;return ratio<.3?'Low load':ratio<.7?'Normal load':'High load';}
  function renderChart(){const samples=electricityState.samples.filter(item=>Number.isFinite(Number(item.power)));if(samples.length<2)return'<div class="electricity-empty">Waiting for recent power samples.</div>';const width=900,height=210,left=44,right=16,top=16,bottom=28,plotW=width-left-right,plotH=height-top-bottom,values=samples.map(item=>Number(item.power)),min=Math.min(...values),max=Math.max(...values),range=Math.max(1,max-min),points=samples.map((item,index)=>[left+index/(samples.length-1)*plotW,top+(1-(Number(item.power)-min)/range)*plotH]),line=points.map(([x,y],index)=>`${index?'L':'M'}${x.toFixed(1)},${y.toFixed(1)}`).join(' '),area=`${line} L${points.at(-1)[0].toFixed(1)},${(top+plotH).toFixed(1)} L${points[0][0].toFixed(1)},${(top+plotH).toFixed(1)} Z`;return`<svg class="electricity-chart" viewBox="0 0 ${width} ${height}" preserveAspectRatio="none" role="img" aria-label="Recent power trend"><line class="axis" x1="${left}" y1="${top+plotH}" x2="${width-right}" y2="${top+plotH}"/><path class="area" d="${area}"/><path class="line" d="${line}"/><text x="4" y="${top+5}">${safe(max.toFixed(1))} W</text><text x="4" y="${top+plotH}">${safe(min.toFixed(1))} W</text><text x="${left}" y="${height-7}">${safe(localTime(samples[0].ts).slice(12))}</text><text text-anchor="end" x="${width-right}" y="${height-7}">${safe(localTime(samples.at(-1).ts).slice(12))}</text></svg>`;}
  function diagnosticValue(key,value){return key.includes('_ts')||key==='last_success'?localTime(value):valueOrNA(value);}
  function render(){const host=document.getElementById('electricityPage');if(!host)return;const payload=electricityState.payload;if(!payload){host.innerHTML='<div class="card"><div class="empty">Electricity data is not available.</div></div>';return;}const d=payload.diagnostics||{},source=String(d.source||'unknown').toLowerCase(),online=payload.health==='healthy'||payload.health==='warning',stale=d.stale===true,max=niceScale(payload.power),gaugePct=Number.isFinite(Number(payload.power))?Math.max(0,Math.min(100,Number(payload.power)/max*100)):0,runtimeIp=d.runtime_ip||d.configured_ip||null,pollLatency=d.poll_latency_ms??d.latency_ms??null;host.innerHTML=`
    <div class="electricity-badges electricity-page-badges">${badge('Meter',online?'Online':payload.health==='offline'?'Offline':'Unknown',online?'ok':payload.health==='offline'?'bad':'warn')}${badge('Data',stale?'Stale':'Fresh',stale?'warn':'ok')}${badge('Source',sourceLabel(source))}${source==='tuya_local'?badge('Mapping',d.mapping_verified===true?'Verified':'Provisional',d.mapping_verified===true?'ok':'warn'):''}</div>
    <div class="electricity-primary-grid">${metricCard('Voltage',payload.voltage,'V')}${metricCard('Current',payload.current,'A')}${metricCard('Active Power',payload.power,'W')}${metricCard('Total Energy',payload.total_energy,'kWh')}</div>
    <div class="electricity-secondary-grid">${metricCard('Status / Health',healthLabel(payload),'',true)}${metricCard('Last Update',localTime(payload.last_update||d.last_success),'',true)}${metricCard('Runtime IP',runtimeIp,'',true)}${metricCard('Poll Latency',pollLatency,pollLatency==null?'':'ms',true)}${metricCard('Data Source',sourceLabel(source),'',true)}</div>
    <div class="electricity-live-grid"><section class="electricity-gauge-card"><div class="card-head"><h2>Live Power</h2></div><div class="electricity-gauge-value">${safe(valueOrNA(payload.power))}${payload.power==null?'':'<small> W</small>'}</div><div class="electricity-gauge-track"><div class="electricity-gauge-fill" style="width:${gaugePct.toFixed(1)}%"></div></div><div class="electricity-gauge-scale"><span>0 W</span><span>${safe(max.toFixed(0))} W dynamic scale</span></div><div class="electricity-load-label">${safe(loadLabel(payload.power,max))}</div></section><section class="electricity-chart-card"><div class="card-head"><h2>Recent Power Trend</h2><small>Last ${electricityState.samples.length} dashboard refresh samples</small></div>${renderChart()}</section></div>
    <details class="electricity-diagnostics"><summary>Advanced Diagnostics</summary><div class="electricity-diagnostics-grid">${DIAGNOSTIC_KEYS.map(key=>`<div class="electricity-diagnostic"><span>${safe(key)}</span><strong>${safe(diagnosticValue(key,d[key]))}</strong></div>`).join('')}</div></details>`;}
  installUi();const originalRefresh=window.refresh,originalRenderPage=window.renderPage;window.refresh=async function refreshWithElectricity(){await Promise.allSettled([originalRefresh(),loadElectricity()]);window.renderPage(window.currentPage());};window.renderPage=function renderPageWithElectricity(page=window.currentPage()){originalRenderPage(page);if(page==='electricity')render();};document.querySelectorAll('[data-nav]').forEach(button=>button.onclick=()=>window.nav(button.dataset.nav));loadElectricity().then(()=>window.renderPage(window.currentPage()));
})();