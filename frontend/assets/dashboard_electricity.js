(() => {
  'use strict';
  if (window.__dashboardElectricityInstalled) return;
  window.__dashboardElectricityInstalled = true;

  const state = {status:null,summary:null,billing:null,history:null,range:'live',series:new Set(['power']),live:[],lastKey:null};
  const safe=value=>window.safeText?window.safeText(value):String(value??'');
  const number=value=>{const n=Number(value);return Number.isFinite(n)?n:null;};
  const sourceName=value=>({tuya_local:'Tuya Local',home_assistant:'Home Assistant',mqtt:'MQTT',unknown:'Unknown'})[String(value||'unknown')]||String(value||'Unknown');
  const healthName=(health,payload)=>health==='healthy'?'Healthy':health==='warning'&&[payload?.voltage,payload?.current,payload?.power,payload?.total_energy].some(v=>number(v)!==null)?'Partial Data':health==='offline'?'Offline':'Unknown';

  function installUi(){document.querySelectorAll('.nav,.mobile-nav').forEach(host=>{if(host.querySelector('[data-nav="electricity"]'))return;const button=document.createElement('button');button.dataset.nav='electricity';button.dataset.short='EL';button.textContent='Electricity';const topology=host.querySelector('[data-nav="topology"]');topology?host.insertBefore(button,topology):host.appendChild(button);});if(document.querySelector('[data-page="electricity"]'))return;const section=document.createElement('section');section.className='page';section.dataset.page='electricity';section.innerHTML='<div id="electricityPage" class="electricity-page"><div class="card"><div class="empty">Electricity data is loading.</div></div></div>';document.querySelector('.main')?.appendChild(section);}
  function epoch(value){if(value===null||value===undefined||value===''||value===0)return null;const n=Number(value);if(Number.isFinite(n))return n>1e12?Math.floor(n/1000):Math.floor(n);const parsed=Date.parse(value);return Number.isFinite(parsed)?Math.floor(parsed/1000):null;}
  function localTime(value){const ts=epoch(value);if(!ts)return'Not available';return new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Bangkok',year:'numeric',month:'short',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(ts*1000));}
  function metric(label,value,unit='',secondary=false){const display=value===null||value===undefined||value===''?'Not available':value;return `<div class="electricity-metric${secondary?' secondary':''}"><span>${safe(label)}</span><strong>${safe(display)}${display!=='Not available'&&unit?`<small>${safe(unit)}</small>`:''}</strong></div>`;}
  function badge(label,value,cls=''){return `<span class="electricity-badge ${cls}">${safe(label)} · ${safe(value)}</span>`;}
  function addLive(payload){const d=payload?.diagnostics||{},ts=epoch(payload?.last_update||d.last_success)||Math.floor(Date.now()/1000),key=`${ts}:${payload?.power}:${payload?.voltage}:${payload?.current}`;if(key===state.lastKey)return;state.lastKey=key;state.live.push({ts,power:number(payload?.power),voltage:number(payload?.voltage),current:number(payload?.current),total_energy:number(payload?.total_energy)});state.live=state.live.slice(-120);}
  async function loadStatus(){try{state.status=await window.get('/api/electricity/status');addLive(state.status);}catch(error){console.error('Electricity status failed',{name:error?.name||'Error'});}}
  async function loadSummary(){try{state.summary=await window.get('/api/electricity/summary');}catch(error){console.error('Electricity summary failed',{name:error?.name||'Error'});}}
  async function loadBilling(){try{state.billing=await window.get('/api/electricity/billing?range=month');}catch(error){console.error('Electricity billing failed',{name:error?.name||'Error'});}}
  async function loadHistory(range=state.range){if(range==='live'){state.history={range:'live',samples:state.live,summary:summarize(state.live)};return;}const apiRange=range==='this_month'?'month':range;try{state.history=await window.get(`/api/electricity/history?range=${encodeURIComponent(apiRange)}`);}catch(error){state.history={range:apiRange,samples:[],summary:{sample_count:0}};console.error('Electricity history failed',{name:error?.name||'Error'});}}
  function summarize(rows){const powers=rows.map(row=>number(row.power)).filter(v=>v!==null);let energy=null;if(rows.length>1){const first=number(rows[0].total_energy),last=number(rows[rows.length-1].total_energy);if(first!==null&&last!==null&&last>=first)energy=last-first;}return{sample_count:rows.length,min_power:powers.length?Math.min(...powers):null,max_power:powers.length?Math.max(...powers):null,avg_power:powers.length?powers.reduce((a,b)=>a+b,0)/powers.length:null,energy_used_kwh:energy};}
  function chartData(){return state.range==='live'?state.live:(state.history?.samples||[]);}
  function seriesDefinitions(){return[{key:'power',label:'Power',unit:'W',cls:'power'},{key:'voltage',label:'Voltage',unit:'V',cls:'voltage'},{key:'current',label:'Current',unit:'A',cls:'current'}].filter(item=>state.series.has(item.key));}
  function renderChart(){const rows=chartData().filter(row=>epoch(row.ts)),series=seriesDefinitions();if(!rows.length||!series.length)return'<div class="electricity-empty">No history samples for this range.</div>';const width=900,height=260,left=58,right=20,top=18,bottom=38,plotW=width-left-right,plotH=height-top-bottom,all=[];series.forEach(item=>rows.forEach(row=>{const v=number(row[item.key]);if(v!==null)all.push(v);}));if(!all.length)return'<div class="electricity-empty">No selected metrics are available.</div>';let min=Math.min(...all),max=Math.max(...all);if(min===max){min-=1;max+=1;}const y=v=>top+(max-v)/(max-min)*plotH,x=i=>rows.length===1?left+plotW/2:left+i/(rows.length-1)*plotW;const paths=series.map(item=>{const parts=[];rows.forEach((row,index)=>{const v=number(row[item.key]);if(v!==null)parts.push(`${parts.length?'L':'M'}${x(index).toFixed(2)},${y(v).toFixed(2)}`);});return `<path class="history-line ${item.cls}" d="${parts.join(' ')}"/>`;}).join('');return `<div class="electricity-history-chart-wrap"><svg id="electricityHistoryChart" class="electricity-history-chart" viewBox="0 0 ${width} ${height}"><line class="axis" x1="${left}" y1="${top+plotH}" x2="${width-right}" y2="${top+plotH}"/>${paths}<g class="history-hover" hidden><line class="history-crosshair" y1="${top}" y2="${top+plotH}"/><g class="history-points"></g></g><rect class="history-hit" x="${left}" y="${top}" width="${plotW}" height="${plotH}" fill="transparent"/></svg><div class="electricity-history-tooltip" hidden></div></div>`;}
  function installChartInteraction(){const svg=document.getElementById('electricityHistoryChart'),wrap=svg?.parentElement,hit=svg?.querySelector('.history-hit'),hover=svg?.querySelector('.history-hover'),cross=svg?.querySelector('.history-crosshair'),points=svg?.querySelector('.history-points'),tip=wrap?.querySelector('.electricity-history-tooltip'),rows=chartData().filter(row=>epoch(row.ts)),series=seriesDefinitions();if(!svg||!hit||!hover||!cross||!points||!tip||!rows.length)return;const box=svg.viewBox.baseVal,left=58,right=(box.width||900)-20,top=18,bottom=(box.height||260)-38,positions=rows.length===1?[(left+right)/2]:rows.map((_,i)=>left+i/(rows.length-1)*(right-left));const pick=x=>{if(positions.length===1)return 0;if(x<=(positions[0]+positions[1])/2)return 0;const last=positions.length-1;if(x>=(positions[last-1]+positions[last])/2)return last;let best=0,dist=Infinity;positions.forEach((p,i)=>{const d=Math.abs(p-x);if(d<dist){dist=d;best=i;}});return best;};const move=event=>{const rect=svg.getBoundingClientRect(),pointer=event.touches?.[0]||event,xSvg=(pointer.clientX-rect.left)/rect.width*(box.width||900),ySvg=(pointer.clientY-rect.top)/rect.height*(box.height||260);if(ySvg<top||ySvg>bottom)return;const index=pick(Math.max(left,Math.min(right,xSvg))),sampleX=positions[index],row=rows[index];cross.setAttribute('x1',sampleX);cross.setAttribute('x2',sampleX);points.innerHTML=series.map(item=>number(row[item.key])===null?'':`<circle class="history-point ${item.cls}" cx="${sampleX}" cy="${top+12}" r="5"/>`).join('');hover.hidden=false;tip.innerHTML=`<strong>${safe(localTime(row.ts))}</strong>${series.map(item=>`<span>${safe(item.label)}: ${number(row[item.key])===null?'Not available':safe(number(row[item.key]).toFixed(2))} ${item.unit}</span>`).join('')}`;tip.hidden=false;const localX=pointer.clientX-wrap.getBoundingClientRect().left,tipW=tip.offsetWidth||190;let leftPx=localX+12;if(leftPx+tipW>wrap.clientWidth-8)leftPx=localX-tipW-12;tip.style.left=`${Math.max(8,Math.min(wrap.clientWidth-tipW-8,leftPx))}px`;tip.style.top='12px';};hit.onpointermove=move;hit.onmousemove=move;hit.ontouchmove=e=>{e.preventDefault();move(e);};hit.onpointerleave=hit.onmouseleave=()=>{hover.hidden=true;tip.hidden=true;};}
  function download(blob,name){const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
  function csvExport(){const rows=chartData(),lines=['timestamp,voltage,current,power,total_energy,source,health',...rows.map(row=>[epoch(row.ts)||'',row.voltage??'',row.current??'',row.power??'',row.total_energy??'',row.source??'',row.health??''].map(v=>`"${String(v).replaceAll('"','""')}"`).join(','))];download(new Blob([lines.join('\n')],{type:'text/csv'}),`electricity-${state.range}.csv`);}
  function pngExport(){const svg=document.getElementById('electricityHistoryChart');if(!svg)return;const image=new Image(),canvas=document.createElement('canvas');canvas.width=1200;canvas.height=420;image.onload=()=>{const ctx=canvas.getContext('2d');ctx.fillStyle='#0d1520';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.drawImage(image,0,0,canvas.width,canvas.height);canvas.toBlob(blob=>blob&&download(blob,`electricity-${state.range}.png`));};image.src='data:image/svg+xml;charset=utf-8,'+encodeURIComponent(new XMLSerializer().serializeToString(svg));}
  function render(){const host=document.getElementById('electricityPage'),payload=state.status;if(!host)return;if(!payload){host.innerHTML='<div class="card"><div class="empty">Electricity data is not available.</div></div>';return;}const d=payload.diagnostics||{},runtimeIp=d.runtime_ip||d.configured_ip||null,pollLatency=d.poll_latency_ms??d.latency_ms??null,source=String(d.source||'unknown'),summary=state.summary||{},historySummary=(state.range==='live'?summarize(state.live):state.history?.summary)||{},billing=state.billing||{},mapping=source==='tuya_local'?badge('Mapping',d.mapping_verified===true?'Verified':'Provisional',d.mapping_verified===true?'ok':'warn'):'';const safeDiag=['mapping_verified','stale','last_success','last_attempt_ts','last_error','consecutive_failures','configured_ip','runtime_ip','auto_discovery','last_scan_ts','last_scan_result','scan_count','poller_started','poller_alive'];host.innerHTML=`<div class="electricity-badges">${badge('Meter',healthName(payload.health,payload),payload.health==='offline'?'bad':payload.health==='healthy'?'ok':'warn')}${badge('Source',sourceName(source))}${mapping}</div><div class="electricity-primary-grid">${metric('Voltage',payload.voltage,'V')}${metric('Current',payload.current,'A')}${metric('Active Power',payload.power,'W')}${metric('Total Energy',payload.total_energy,'kWh')}</div><div class="electricity-secondary-grid">${metric('Status / Health',healthName(payload.health,payload),'',true)}${metric('Last Update',localTime(payload.last_update||d.last_success),'',true)}${metric('Runtime IP',runtimeIp,'',true)}${metric('Poll Latency',pollLatency,pollLatency==null?'':'ms',true)}${metric('Data Source',sourceName(source),'',true)}</div><section class="electricity-history-card"><div class="card-head"><div><h2>Electricity History</h2><small>Persistent backend history</small></div><div><button class="btn ghost" data-electricity-export="csv">CSV</button><button class="btn ghost" data-electricity-export="png">PNG</button></div></div><div class="electricity-range-buttons">${[['live','Live'],['24h','24H'],['7d','7D'],['30d','30D'],['this_month','This Month']].map(([key,label])=>`<button class="btn ghost ${state.range===key?'active':''}" data-electricity-range="${key}">${label}</button>`).join('')}</div><div class="electricity-series-toggles">${[['power','Power'],['voltage','Voltage'],['current','Current']].map(([key,label])=>`<label><input type="checkbox" data-electricity-series="${key}" ${state.series.has(key)?'checked':''}> ${label}</label>`).join('')}</div>${renderChart()}<div class="electricity-history-summary">${metric('Minimum Power',historySummary.min_power,'W',true)}${metric('Maximum Power',historySummary.max_power,'W',true)}${metric('Average Power',historySummary.avg_power,'W',true)}${metric('Energy Used',historySummary.energy_used_kwh,'kWh',true)}</div></section><section class="electricity-cost-card"><div class="card-head"><div><h2>Electricity Cost</h2><small>Estimated from configured tariff</small></div></div>${billing.configured===false?'<div class="electricity-empty">Tariff not configured. Add ELECTRICITY_TARIFF_CONFIG_JSON and restart the service.</div>':`<div class="electricity-cost-grid">${metric('Usage Today',summary.today_kwh,'kWh',true)}${metric('Usage This Month',summary.month_kwh,'kWh',true)}${metric('Projected Month-End',summary.estimated_month_end_kwh,'kWh',true)}${metric('Estimated Bill This Month',billing.total,'THB',true)}</div><div class="electricity-billing-breakdown">${[['Energy charge',billing.base_energy_charge],['Ft',billing.ft_charge],['Service charge',billing.service_charge],['VAT',billing.vat],['Total',billing.total]].map(([label,value])=>`<div><span>${label}</span><strong>${value==null?'Not available':`${Number(value).toFixed(2)} THB`}</strong></div>`).join('')}</div><div class="electricity-note">${safe(billing.tariff_name||'Configured tariff')} · ${safe(billing.effective_date||'No effective date')} · Estimated from configured tariff. This is not an official utility invoice.</div>`}</section><details class="electricity-diagnostics"><summary>Advanced Diagnostics</summary><div class="electricity-diagnostics-grid">${safeDiag.map(key=>`<div class="electricity-diagnostic"><span>${safe(key)}</span><strong>${safe(key.includes('_ts')||key==='last_success'?localTime(d[key]):d[key]??'Not available')}</strong></div>`).join('')}</div></details>`;bind();installChartInteraction();}
  function bind(){document.querySelectorAll('[data-electricity-range]').forEach(button=>button.onclick=async()=>{state.range=button.dataset.electricityRange;await loadHistory();render();});document.querySelectorAll('[data-electricity-series]').forEach(input=>input.onchange=()=>{input.checked?state.series.add(input.dataset.electricitySeries):state.series.delete(input.dataset.electricitySeries);render();});const csv=document.querySelector('[data-electricity-export="csv"]'),png=document.querySelector('[data-electricity-export="png"]');if(csv)csv.onclick=csvExport;if(png)png.onclick=pngExport;}
  installUi();const originalRefresh=window.refresh,originalRenderPage=window.renderPage;window.refresh=async function refreshWithElectricity(){await Promise.allSettled([originalRefresh(),loadStatus(),loadSummary(),loadBilling(),state.range==='live'?Promise.resolve():loadHistory()]);window.renderPage(window.currentPage());};window.renderPage=function renderPageWithElectricity(page=window.currentPage()){originalRenderPage(page);if(page==='electricity')render();};document.querySelectorAll('[data-nav]').forEach(button=>button.onclick=()=>window.nav(button.dataset.nav));Promise.allSettled([loadStatus(),loadSummary(),loadBilling(),loadHistory()]).then(()=>window.renderPage(window.currentPage()));
})();
